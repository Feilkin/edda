// Sample edda script

let hello = -> { print "hello"; print "      world!"; };
hello();

let greeting = "hello, world!" ;
let get_greeting = -> { greeting };

print greeting;

let print_greeting = (name) -> { print "hello, "; print name; };
print_greeting("world!");

let fibonacci = (n) -> {
	if n <= 1 { n }
	else { fibonacci(n - 2) + fibonacci(n - 1) }
};

for i in 1 .. 20 {
	print fibonacci(i);
}